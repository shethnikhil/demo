# Optional: override default price change threshold (default: 5.0)
# PRICE_CHANGE_THRESHOLD=5.0

# Optional: override check interval in minutes (default: 15)
# CHECK_INTERVAL_MINUTES=15

# Optional: max news articles to fetch per alert (default: 5)
# NEWS_MAX_RESULTS=5
